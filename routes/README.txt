*** create a separate js file for every route ***

Naming Format:
rc_ for customerRoutes
ra_ for adminRoutes
rr_ for restaurantRoutes

Examples:
    rc_getAccount
    ra_getReports
    rr_createAccount

